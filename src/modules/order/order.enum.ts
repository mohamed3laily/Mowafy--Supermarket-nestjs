export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum OrderStatus {
  ACTIVE,
  INACTIVE,
  PENDING,
  COMPLETED,
  CANCELLED,
}

export enum UserStatus {
  ACTIVE,
  INACTIVE,
}
export enum PaymentMethod {
  CASH,
  CARD,
  PAYPAL,
}
